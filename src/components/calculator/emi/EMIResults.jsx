export default function EMIResults({
  currency,
  emi,
  totalPayment,
  totalInterest,
  loanAmount,
  formatCurrency,
}) {
  return (
    <div>
      {/* Move the entire RIGHT PANEL here */}
    </div>
  );
}